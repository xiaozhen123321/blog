# ====================================
# 生产环境配置示例（Railway）
# ====================================

NODE_ENV=production

# Railway 会自动注入 PORT
# PORT=<自动分配>

# JWT 配置（必须修改！）
# 使用命令生成随机密钥：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=<请使用上述命令生成>
JWT_EXPIRES_IN=7d

# CORS 配置（部署前端后填入实际 URL）
FRONTEND_URL=https://blog-fronted-gamma.vercel.app
ADMIN_URL=https://blog-admin-five-xi.vercel.app

# 文件上传配置
MAX_FILE_SIZE=5242880
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/gif,image/webp

# 数据库配置（Railway MySQL 自动注入）
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_USER=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}
DB_NAME=${MYSQLDATABASE}
