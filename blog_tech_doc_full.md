# 博客网站项目技术文档（完整版：含全部主要功能，前端使用 Ant Design）

## 📦 项目结构总览

本项目共包含三个独立的 package，分别负责前端展示、后台管理，以及后端服务：

```
root/
 ├─ packages/
 │   ├─ blog-frontend/      # 博客前台页面（公开访问）
 │   ├─ blog-admin/         # 后台管理页面（文章发布/维护）
 │   └─ blog-server/        # Node.js 后端服务（Express + MySQL）
 └─ ...
```

---

# 1. 🎨 前端页面（blog-frontend）

## 📘 技术栈
- React Hooks
- TypeScript
- MobX（状态管理）
- CSS Modules（样式方案）
- **Ant Design（UI 组件库）**
- Rsbuild（构建工具）
- ky（接口通信）

## 📁 主要功能

### 🌐 公共页面
- **首页展示文章列表**
  - 支持分页
  - 支持按发布时间排序
  - 支持热门推荐位（可选）
- **文章详情页**
  - 展示文章标题、正文、封面图、发布时间
  - 支持评论区（可扩展）
- **文章分类页**
  - 分类列表展示
  - 每个分类下的文章分页展示
- **搜索功能**
  - 根据关键词匹配文章标题/摘要
- **全局 UI 布局**
  - Header（导航栏）
  - Footer（版权信息）
  - 响应式布局（AntD Grid）
- **SEO 友好处理（可选）**
  - 动态 Title、Description
  - 文章内容 Schema 数据（可扩展）

---

# 2. 🛠️ 后台管理页面（blog-admin）

## 📘 技术栈
- React Hooks
- TypeScript
- MobX
- CSS Modules
- **Ant Design（后台 UI 框架）**
- Draft.js（富文本编辑器）
- Rsbuild
- ky

## 📁 主要功能

### 🔐 权限系统
- 登录页
- Token 校验
- 权限管理（基础版：只有管理员用户），可以创建用户

### 📝 文章管理
- **文章列表**
  - 分页、搜索、筛选分类
  - 编辑、删除文章
- **文章发布页面**
  - 标题输入
  - 文章封面图上传（AntD Upload）
  - 分类选择
  - 文章摘要自动生成或手动填写
  - **富文本编辑器（Draft.js 封装）**
  - 保存草稿
  - 发布文章

### 🗂 分类管理
- 分类列表
- 新建分类
- 编辑分类名称
- 删除分类（需检查是否存在文章）

### 🖼 图片与资源管理（可选）
- 图片上传接口对接

---

# 3. 🚀 后端服务（blog-server）

## 📘 技术栈
- Node.js
- Express.js
- MySQL
- mysql2（或 Sequelize）
- JWT 鉴权（如需要后台登录）
- Multer（文件上传）
- ESLint

## 📁 主要功能

### 🔐 用户认证
- 登录接口
- JWT Token 生成与校验

### 📝 文章管理 API
- GET `/api/articles`（分页、分类、搜索）
- GET `/api/articles/:id`
- POST `/api/articles`（创建文章）
- PUT `/api/articles/:id`（编辑文章）
- DELETE `/api/articles/:id`

### 🗂 分类管理 API
- GET `/api/categories`
- POST `/api/categories`
- PUT `/api/categories/:id`
- DELETE `/api/categories/:id`

### 🖼 文件上传
- POST `/api/upload`  
  用于上传文章封面图等资源

### 📊 其他（可扩展）
- 访问日志记录
- 管理员操作日志
- Cache 层（Memory / Redis）

---

# 4. 🗄️ 数据库设计（MySQL）

## `articles`（文章表）
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | int PK AI | 主键 |
| title | varchar(255) | 标题 |
| content | longtext | 富文本内容 |
| summary | text | 摘要 |
| category_id | int | 分类 id |
| cover_url | varchar(255) | 封面图 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

## `categories`（分类表）
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | int PK AI | 主键 |
| name | varchar(255) | 分类名 |
| created_at | datetime | |
| updated_at | datetime | |

## `admin_user`（后台用户表）
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | int PK AI |
| username | varchar(50) |
| password | varchar(255)（hash） |
| created_at | datetime |

---

# 5. 📏 项目规范

## 通用规范
- 使用 **ESLint** 作为代码规范工具
- TypeScript 使用 **strict 模式**
- 禁止 any（除极特殊情况）
- API 统一返回格式：
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

---

# 6. 🧩 包之间的通信关系

```
blog-frontend  --->  blog-server (REST API)
blog-admin     --->  blog-server (REST API)
blog-server    --->  MySQL
```

---

# 7. 🚀 开发与运行

## 前端（blog-frontend）
```
cd packages/blog-frontend
npm install
npm run dev
```

## 后台管理（blog-admin）
```
cd packages/blog-admin
npm install
npm run dev
```

## 后端（blog-server）
```
cd packages/blog-server
npm install
npm start
```

